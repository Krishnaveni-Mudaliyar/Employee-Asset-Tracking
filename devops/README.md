# DevOps

This folder owns reusable CI/CD and release assets: pipeline templates, signing guidance, environment configuration examples, and release checklists.

Keep secrets in the CI/CD platform's secret store. Never commit certificate files, client secrets, access tokens, or production configuration values.
